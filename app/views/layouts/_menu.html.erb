<% if user_signed_in? %>
  | <%= link_to "profile", user_profile_path, :class => "button" %>
  | <%= link_to "sign out", destroy_user_session_path, method: :delete, :class => "button"  %>
<% else %>
  | <%= link_to "sign up", new_user_registration_path, :class => "button"  %>
  | <%= link_to "sign in", new_user_session_path, :class => "button"  %>
  <% if studio_signed_in? %>
  | <%= link_to "profile", studio_profile_path, :class => "button"  %>
  | <%= link_to "sign out", destroy_studio_session_path, method: :delete, :class => "button"  %>
  <% else %>
  | <span>are you a <span class="button"><%= link_to "yoga studio", new_studio_session_path, :class => "button"  %>?</span></span>
  <% end %>
<% end %>